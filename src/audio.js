import Browser from './utils/browser'

export class Audio {

    constructor(config) {
        let b = new Browser.Browser();
        this._config = config;
        this._browser = b.name
    }

    static Event() {

    }
}